<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CityRadio" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#546E7A</item>
        <item name="colorPrimaryVariant">#455A64</item>
        <item name="colorOnPrimary">@color/logo_background</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">#546E7A</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:itemBackground">@color/logo_background</item>
        <item name="popupMenuBackground">@color/logo_background</item>
        <item name="android:popupBackground">@color/logo_background</item>
        <item name="android:actionMenuTextColor">@color/logo_red</item>
        <item name="android:textColor">@color/logo_red</item>

        <item name="android:actionBarStyle">@style/CustomActionBarTitleTextStyle</item>
        <item name="android:navigationBarColor">@color/black</item>

    </style>

    <style name="YourCityRadio">
        <item name="android:fontFamily">@font/font_family</item>
    </style>

    <style name="CustomActionBarTitleTextStyle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="fontFamily">@font/oswald_variable_font_wght</item>
        <item name="android:textColor">@color/logo_background</item>
    </style>

    <style name="CustomActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="titleTextStyle">@style/CustomActionBarTitleTextStyle</item>
        <item name="android:textColor">@color/logo_background</item>
    </style>

</resources>